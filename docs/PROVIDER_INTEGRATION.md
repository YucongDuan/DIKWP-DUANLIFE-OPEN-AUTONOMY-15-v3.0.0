# Provider Integration

The core exports a provider-neutral JSON request containing the system constitution, mission question, purpose, local context, and authority boundary. It performs no network call.

A deployment may add a model-specific connector under an explicit network capability. Provider outputs remain proposals until verified against local evidence, plural worlds, and mission constraints. The provider never becomes the source of identity or authority.
