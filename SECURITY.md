# Security Policy

The reference core has no network client, shell executor, automatic publisher, payment connector, voting connector, or contract-signing connector.

Report vulnerabilities with a minimal reproducible case. Do not include private memory, credentials, or personal records in public issues.

For untrusted extensions, use a container or virtual machine, minimal credentials, resource limits, and independent review.
